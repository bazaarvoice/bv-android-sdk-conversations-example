<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.conversations">

  <application
    android:name="com.example.conversations.App"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name="com.example.conversations.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity
      android:name="com.example.conversations.reviews.DisplayReviewsActivity"
      android:label="@string/title_activity_display_reviews">
    </activity>
    <activity
      android:name="com.example.conversations.reviews.SubmitReviewsActivity"
      android:label="@string/title_activity_submit_review">
    </activity>
  </application>

</manifest>